# §6.1 — Vue logique

*Dernière mise à jour : 13/05/2026*

Cette section présente l'architecture de SupEvents à travers deux niveaux du modèle C4 : le diagramme Context, qui situe le système dans son environnement humain et technique, et le diagramme Containers, qui détaille les blocs déployables qui composent SupEvents. Ces deux vues sont destinées aux développeurs, aux architectes et aux décideurs techniques qui rejoignent le projet.

---

## §6.1.1 — Diagramme C4 Context

Ce diagramme donne une vue "grand angle" du système SupEvents : qui l'utilise, avec quels systèmes tiers il interagit. Il ne descend pas dans les détails internes mais permet de comprendre immédiatement le périmètre et les dépendances externes. Il est destiné à tout interlocuteur technique ou métier qui souhaite comprendre le positionnement du système sans entrer dans son architecture.

Le point notable est la séparation stricte entre les acteurs humains (Étudiant, Organisateur, Administrateur) et les systèmes tiers automatisés (Stripe, SendGrid, SSO École, CDN). Les interactions humaines passent toutes par le frontend SPA, tandis que les interactions avec les systèmes tiers sont orchestrées côté backend — à l'exception du CDN qui sert directement les assets statiques.

```mermaid
flowchart TD
    actor_etudiant(["👤 Étudiant\n[Personne]"])
    actor_organisateur(["👤 Organisateur\n[Personne]"])
    actor_admin(["👤 Administrateur\n[Personne]"])

    system_supevents["🎓 SupEvents\n[Système logiciel]"]

    ext_stripe["💳 Stripe\n[Système externe]"]
    ext_sendgrid["📧 SendGrid\n[Système externe]"]
    ext_sso["🔐 SSO École\n[Système externe]"]
    ext_cdn["🌐 CDN\n[Système externe]"]

    actor_etudiant -- "HTTPS/REST\nConsulte le catalogue,\ns'inscrit aux événements" --> system_supevents
    actor_organisateur -- "HTTPS/REST\nCrée et gère\nses événements" --> system_supevents
    actor_admin -- "HTTPS/REST\nSupervise la plateforme,\nvalide les organisateurs" --> system_supevents

    system_supevents -- "HTTPS/REST\nInitie le paiement,\ncréation PaymentIntent" --> ext_stripe
    ext_stripe -- "Webhook HTTPS\nConfirme ou signale\nl'échec de la transaction" --> system_supevents

    system_supevents -- "HTTPS/REST\nEnvoi d'emails\ntransactionnels" --> ext_sendgrid

    actor_etudiant -- "OIDC/OAuth2\nAuthentification\nvia identifiants ENT" --> ext_sso
    ext_sso -- "OIDC/OAuth2\nCallback avec\ncode d'autorisation" --> system_supevents

    system_supevents -- "HTTPS\nUpload des médias\nd'événements" --> ext_cdn
    ext_cdn -- "HTTPS\nDiffusion des médias\naux utilisateurs" --> actor_etudiant

    style system_supevents fill:#1168BD,color:#fff,stroke:#0b4884
    style actor_etudiant fill:#08427B,color:#fff,stroke:#073763
    style actor_organisateur fill:#08427B,color:#fff,stroke:#073763
    style actor_admin fill:#08427B,color:#fff,stroke:#073763
    style ext_stripe fill:#999999,color:#fff,stroke:#6b6b6b
    style ext_sendgrid fill:#999999,color:#fff,stroke:#6b6b6b
    style ext_sso fill:#999999,color:#fff,stroke:#6b6b6b
    style ext_cdn fill:#999999,color:#fff,stroke:#6b6b6b
```

---

## §6.1.2 — Diagramme C4 Containers

Ce diagramme zoome à l'intérieur de SupEvents et expose les blocs déployables qui le composent, leurs technologies et les protocoles de communication qui les relient. Il est la référence principale pour les développeurs qui configurent l'environnement, les devops qui orchestrent le déploiement, et les architectes qui évaluent les points de couplage.

L'architecture suit un pattern classique à deux niveaux : un frontend SPA servi par CDN, un backend monolithique modulaire exposé via API Gateway. Le découplage asynchrone entre le backend et le service de notifications est assuré par RabbitMQ — choix documenté dans ADR-001. Les flux synchrones (HTTPS, TCP) sont distingués des flux asynchrones (AMQP) par le style des flèches.

```mermaid
flowchart TD
    actor(["👤 Utilisateurs\n[Étudiant / Organisateur / Admin]"])
    ext_stripe["💳 Stripe\n[Système externe]"]
    ext_sendgrid["📧 SendGrid\n[Système externe]"]
    ext_sso["🔐 SSO École OIDC\n[Système externe]"]
    ext_s3["🗂️ S3 / MinIO\n[Stockage objets]"]

    subgraph supevents ["🎓 SupEvents [Système logiciel]"]
        frontend["🖥️ Frontend Web\n[SPA — React]\nInterface utilisateur"]
        gateway["🔀 API Gateway\n[nginx / Kong]\nRoutage, SSL termination,\nrate limiting"]
        backend["⚙️ Service Backend\n[API REST — NestJS]\nLogique métier modulaire"]
        db[("🗄️ Base de données\n[PostgreSQL :5432]\nDonnées persistantes")]
        cache[("⚡ Cache\n[Redis :6379]\nSessions, idempotence,\nrate limiting")]
        broker["📨 Broker de messages\n[RabbitMQ :5672]\nÉvénements asynchrones"]
        notif["🔔 Service Notifications\n[Worker Node.js]\nEnvoi emails, in-app"]
    end

    actor -- "HTTPS :443" --> frontend
    frontend -- "HTTPS :443" --> gateway
    gateway -- "HTTP :3000" --> backend

    backend -- "TCP :5432" --> db
    backend -- "Redis :6379" --> cache
    backend -- "HTTPS :443\nPaymentIntent, capture" --> ext_stripe
    ext_stripe -- "Webhook HTTPS :443" --> gateway
    backend -- "OIDC callback\nHTTPS :443" --> ext_sso
    backend -- "HTTPS :443\nUpload médias" --> ext_s3

    backend -. "AMQP :5672\n[async] publish events" .-> broker
    broker -. "AMQP :5672\n[async] consume events" .-> notif
    notif -- "HTTPS :443\nSMTP via API" --> ext_sendgrid

    style frontend fill:#85BBF0,color:#000,stroke:#5a9fd4
    style gateway fill:#F0A500,color:#000,stroke:#c88400
    style backend fill:#1168BD,color:#fff,stroke:#0b4884
    style db fill:#336791,color:#fff,stroke:#1d4d6e
    style cache fill:#DC382D,color:#fff,stroke:#a82820
    style broker fill:#FF6600,color:#fff,stroke:#cc5200
    style notif fill:#85BBF0,color:#000,stroke:#5a9fd4
```

---

## §6.1.3 — Diagramme de composants — API Backend

Ce diagramme décompose l'intérieur du service backend en modules fonctionnels. Il montre les interfaces exposées par chaque module, leurs dépendances entre eux et vers les systèmes tiers. Il est la référence pour les développeurs qui implémentent un module : ils voient immédiatement quels autres modules ils peuvent appeler et lesquels sont hors de leur périmètre.

La décision architecturale clé visible ici est le découplage entre `TicketModule` et `NotificationModule` via RabbitMQ. `TicketModule` n'appelle jamais directement `NotificationModule` — il publie un événement et reprend son exécution sans attendre. Cela garantit que l'envoi d'email ne bloque jamais le parcours d'inscription.

```mermaid
flowchart LR
    gw["API Gateway"]

    subgraph backend ["API Backend [NestJS]"]
        auth["AuthModule\nIAuthService"]
        event["EventModule\nIEventService"]
        ticket["TicketModule\nITicketService"]
        payment["PaymentModule\nIPaymentService"]
        notif["NotificationModule\nINotificationService"]
        user["UserModule\nIUserService"]
    end

    pg[("PostgreSQL")]
    redis[("Redis")]
    rabbit["RabbitMQ"]
    stripe["Stripe API"]
    sso["SSO École"]
    s3["S3 / MinIO"]

    gw --> auth
    gw --> event
    gw --> ticket
    gw --> payment
    gw --> user

    auth --> sso
    auth --> redis
    auth --> user

    ticket --> event
    ticket --> payment
    ticket --> pg
    ticket -. "publish\nticket.confirmed\npayment.failed" .-> rabbit

    payment --> stripe
    payment --> pg
    payment --> redis

    event --> pg
    event --> s3

    user --> pg

    rabbit -. "consume events" .-> notif
    notif --> pg
```
